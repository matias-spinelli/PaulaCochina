import { HeartAnimateDirective } from './heart-animate.directive';

describe('HeartAnimateDirective', () => {
  it('should create an instance', () => {
    const directive = new HeartAnimateDirective();
    expect(directive).toBeTruthy();
  });
});
