import { FormatIngredientPipe } from './format-ingredient.pipe';

describe('FormatIngredientPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatIngredientPipe();
    expect(pipe).toBeTruthy();
  });
});
