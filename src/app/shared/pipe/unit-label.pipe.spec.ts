import { UnitLabelPipe } from './unit-label.pipe';

describe('UnitLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new UnitLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
